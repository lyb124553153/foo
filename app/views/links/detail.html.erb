<div class="card-footer">
  <div class="alert alert-success">
    <b>Shortened URL: </b><%= link_to "#{@link.shortened_url}", @link.shortened_url %>
    <b>Origin URL: </b> <%= link_to 'detail', "links/#{@link.obfuscated_code}" %>
    <b> used times <%= @link.use_count %></b>
  </div>
</div>